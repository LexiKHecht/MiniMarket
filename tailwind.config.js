/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    // make sure to add content destinations
  ],
  theme: {
    extend: {
      // custom styles
    },
    colors: {
      // Light mode colors
      // Dark mode colors
    },
  },
  plugins: [require("daisyui")],
};
